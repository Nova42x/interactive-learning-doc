---
name: interactive-learning-doc
description: Use when 用户要把技术内容做成图文互动式 HTML 学习文档（"做成网页带我读"）。
version: 1.0.0
author: Hermes
license: MIT
metadata:
  hermes:
    tags: [html, tutorial, documentation, teaching, interactive]
    related_skills: [claude-design, popular-web-designs]
---

# 互动式学习文档生成器（单文件 HTML，固定 UI）

把**任何技术内容**（论文、项目源码、教程、技术文档、工具使用说明）做成**单文件 HTML 学习文档**，面向零基础用户：由浅入深的章节 + 手绘 SVG 图解 + 可交互的代码块（悬停某行 → 框选高亮 + 右侧面板显示该行解释）+ 小测验 + 词汇表。已验证（640KB 大型单文件文档，微调后已通用化）。

## When to Use

- 用户说"把这个做成网页带我读"、"给我做个学习页面"、"图文并茂的阅读文档"、"做个教程 HTML"
- 需要把论文/仓库/教程转成可交互的阅读材料（面试/组会/教学/自学场景）
- 素材是任何有知识结构的文本类内容（代码、文档、论文、工具说明均可）

## 适用素材（不限于代码）

| 素材类型 | 例子 | 教学侧重 |
|---|---|---|
| 论文 | 方法学论文、综述 | 概念→方法→结果→差异点 |
| 项目源码 | 参考仓库、论文复现 | 文件地图→数据流→核心代码→训练/部署 |
| 教程/技术文档 | 框架教程、API 文档 | 概念→快速上手→进阶→常见坑 |
| 工具使用 | CLI 工具、软件 | 安装→核心命令→参数→实战案例 |
| 面试速览 | 八股、知识体系 | 概念卡→类比→小测验 |

触发词："把这个做成网页带我读"、"给我做个学习页面"、"图文并茂的阅读文档"、"做个教程 HTML"。

## 工作流

### 1. 先读透素材（不要跳步）
读 README/正文/核心源码，记下：内容骨架、关键概念、流程、参数/数字、易混点。"易混点"和"与直觉相悖处"是文档最值钱的部分。

### 2. 章节结构：按素材自适应生成（不要套固定模板）
**章节由素材本身决定，不是固定清单。** 方法：
1. **提取素材骨架**：论文 → 摘要→方法→结果→讨论；仓库 → 数据→特征→模型→训练；教程 → 概念→上手→进阶→实战；工具 → 输入→处理→输出
2. **教学重构**：按"零基础能跟上"的认知顺序重排：
   - 开篇：背景 + 为什么需要它（用类比，不用术语定义）
   - 中段：必备概念 → 整体架构 → 核心内容（逐行/逐节）→ 细节
   - 收尾：结果/效果对照 → 文档与实际差异 → 对读者的行动建议 → 小测验
3. 章节数量自适应（4~12 章）；每个"教学单元"（概念/图解/代码块/对比）都值得独立成节
4. **固定的是教学单元，不是章节标题**：图解、代码块、提示框、小结、测验这些"零件"始终要有，每章放什么由内容决定

### 3. UI 样式：固定使用标准主题（禁止自由发挥配色/布局）
视觉与「已验证的大型单文件文档」完全一致，**直接套用**：

- **样式文件**：`references/ui-theme.css`（完整主题，含全部组件样式，直接内嵌到 `<style>`）
- **骨架模板**：`references/skeleton.html`（复制即用的完整骨架：head + 全部 CSS + sidebar + hero + 每种组件一个示例 + 全部 JS，改标题和内容即可）
- **设计令牌**（不变更）：纸感米白背景 `--paper:#f6f7f4` / 绿色主色 `--accent:#0e7c5b`；左侧 264px 目录 sidebar + 右侧 330px 悬停解释面板；内容单栏居中；代码块深色 `#161d26`；callout 五色（蓝=发现、黄=坑、绿=类比、灰=细节、紫=行动）；圆角 12~14px；正文 PingFang SC/微软雅黑
- **禁止**：自定义配色、改布局、加外部依赖（CDN 库、在线字体）。保持单文件离线可用（图片 base64 内嵌）
- 用户明确提出换风格时才允许偏离

### 4. 核心功能（三件套，从 references 复制）
- **CSS**：`ui-theme.css` 已含 `.cline` 行悬停框选 + `#line-hint` 解释面板样式
- **高亮引擎**：`skeleton.html` 内嵌的 JS（全占位符方案：字符串/数字/关键字/函数/装饰器各自占位，最后统一还原——防正则互相污染）
- **逐行解释数据**：`LINE_EXPLAIN = {'cb-xxx': {行号: '解释'}}` 对象 + 交互 JS（hover 显示、click 锁定/解锁、无解释行兜底文案、<1180px 隐藏面板）
- 所有 JS 放同一个 `<script>`，任何一个语法错误会中断全部功能

### 5. 代码块结构（codewrap 约定）
```html
<div class="codewrap" id="cb-attn">
  <div class="code-head">…<span class="fname">文件名 · 说明</span></div>
  <pre class="code">纯文本，不带任何 span</pre>
</div>
```
- **每个代码块必须有唯一 id**（`cb-xxx`），逐行解释按 id 索引
- pre 内必须**纯文本**（行号/高亮由 JS 生成）；只对核心代码块（5~8 个）写逐行解释
- 非代码类文档（纯教程无代码）可跳过此功能，其余组件照用

### 6. 必须的校验流程（缺一不可）
```bash
# ① JS 语法检查（先于浏览器验证！）
node --check extracted-script.js   # 提取 <script> 内容后检查

# ② 行号对齐核对（解释键数 vs 代码块实际行数）——有代码块时必跑
python references/check-explain-lines.py guide.html
```
- ③ 浏览器实测：悬停核心代码块第 1 行和最后 1 行，解释与代码对应；console 零错误
- ④ 截图视觉确认：框选高亮、面板不遮挡内容

### 7. 收尾
- 文档放在对应的项目/仓库目录内；副本/备份同步更新
- `open_preview` 打开给用户看；提醒 Ctrl+F5 强刷防缓存

## 已知坑（全部踩过，直接规避）

1. **语法高亮正则互相污染**：绝不能在已生成的 `<span class="st">` 上再跑关键字正则（`class` 被二次包裹 → `class="st">` 乱码）。必须全占位符方案（见 skeleton.html 内注释）。
2. **行号错位**：写逐行解释按"逻辑行"编号（漏算注释行/参数换行/空行）→ 整体偏移。**必跑核对脚本**。
3. **JS 拼接错误**：脚本生成 JS 数据时对象结尾自带逗号又追加逗号 → `},,` 整个 script 崩溃。写完必跑 `node --check`。
4. **CSS 伪元素图标 + 文本图标重复**：`.co-find::before{content:"🔍"}` 与标题文本 `🔍 秘密 ①` 叠加 → 双图标。图标只走一条路（伪元素），co-title 文本里不写 emoji。
5. **长行溢出**：行元素用 `display:block;width:max-content;min-width:100%`（inline-block width:100% 会让滚动条失效）。
6. **CRLF 文件**：Windows 下 HTML 是 `\r\n`，Python 正则按 `\n` 匹配失败 → 用 `re.S` + 容忍 `\r`，或按块边界定位。
7. **file:// 下 console 错误 message 为空**：用 `node --check` 定位语法错误；功能验证靠 DOM 断言。
8. **IIFE 链中断**：任何 IIFE 抛错会中断后续所有 IIFE（quiz 挂 = 前面的 scrollspy 或进度条出错，反向排查）。

## 验证清单（交付前）

- [ ] node --check 通过
- [ ] 行号核对脚本全部 ✓（有代码块时）
- [ ] 浏览器 console 无错误（或确认是 file:// 无害误报）
- [ ] 悬停核心代码块第 1 行和最后 1 行，解释对应
- [ ] 代码块可见文本无 `class="` 残留
- [ ] 提示框图标无重复
- [ ] 长行不溢出代码块框架
- [ ] UI 与标准主题一致（未自由发挥）
- [ ] 单文件、离线可用（图片 base64 内嵌）